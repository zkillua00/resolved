[package]
name = "resolved-release"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false
description = "Shared, offline release-feed validation for Resolved"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
semver = "1.0"
url = "2.5"
